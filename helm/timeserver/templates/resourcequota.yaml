{{- if .Values.resourceQuota.enabled }}
# ResourceQuota applies to the entire namespace, not just this release.
# If multiple services share this namespace, all of them count against these limits.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "timeserver.fullname" . }}
  labels:
    {{- include "timeserver.labels" . | nindent 4 }}
spec:
  hard:
    requests.cpu: {{ .Values.resourceQuota.requests.cpu | quote }}
    requests.memory: {{ .Values.resourceQuota.requests.memory | quote }}
    limits.cpu: {{ .Values.resourceQuota.limits.cpu | quote }}
    limits.memory: {{ .Values.resourceQuota.limits.memory | quote }}
    pods: {{ .Values.resourceQuota.maxPods | quote }}
{{- end }}
